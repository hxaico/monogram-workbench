# Monogram Workbench

A small collection of data-related experiments and one-off projects. This repo is a workspace for testing ideas, running evaluations, and keeping project-specific code together. This repo is intentionally flexible and may evolve as new projects are added.

## Current Projects

- `search-evals/`: Benchmarks web search API performance by running query sets across multiple gateways and saving raw responses.
