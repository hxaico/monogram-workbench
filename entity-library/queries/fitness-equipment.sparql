# Fitness equipment - types only (not specific products)
SELECT DISTINCT ?item ?itemLabel WHERE {
  ?item wdt:P279+ wd:Q352222 .  # subclass of exercise equipment (types only)
  ?item rdfs:label ?itemLabel .
  FILTER(LANG(?itemLabel) = "en")
  FILTER(!REGEX(?itemLabel, "^Q[0-9]+$"))
}
LIMIT 500
