# Building materials - types only  
SELECT DISTINCT ?item ?itemLabel WHERE {
  ?item wdt:P279+ wd:Q206615 .  # subclass of building material
  ?item rdfs:label ?itemLabel .
  FILTER(LANG(?itemLabel) = "en")
  FILTER(!REGEX(?itemLabel, "^Q[0-9]+$"))
}
LIMIT 500
